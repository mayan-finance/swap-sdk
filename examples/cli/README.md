# Mayan Swap SDK cli example

This is a simple cli example of using the Mayan swap SDK to swap cross chain from Solana or other EVM chains.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run main.ts
```
